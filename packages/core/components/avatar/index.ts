import Avatar, { type AvatarProps } from '$/core/components/avatar/avatar';
import Stack, { type AvatarStackProps } from '$/core/components/avatar/avatar-stack';

export type { AvatarProps, AvatarStackProps };

export default Object.assign(Avatar, { Stack });
